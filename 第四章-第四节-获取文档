	可以通过基本分片和它的复制分片获取一个文档，步骤如下：
	1：客户端向一个节点发送一个GET请求
	2：协作结点通过文档的id的哈希值算出文档所属的分片，由于此分片在所有节点上均有，在这个情况下，它将请求转发给另外一个节点
	3：目标节点将文档返回给协作节点，协作节点再返回给客户端
	对于读请求，协作节点会通过负载均衡算法选择一个分片；默认是轮询算法。
	一个文档已经被索引到基本分片，但还没有被分发到复制分片的时候被请求，这种情况下，复制分片可能会返回文档不存在，而基本分片却返回成功。当索引请求成功返回给用户之后，文档获取情况就会一致了。
